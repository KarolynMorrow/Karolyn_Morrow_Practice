package java_files;

public class Notes {
    /*
    *Absolute path(C:\\, D:\\) Avoid using absolute path!
    * vs
    *Relative path (locate the file from within the current directory)
    *
    *
    * File class methods
    * List() returns an array of the files
    * getParentFile() returns a file pointing to the directory that contains the current file/directory
    * getAbsoluteFile() Returns another instance of file with absolute path
    * toURI()
    * isFile() and isDirectory() Tells if a file points to a file/directory
    * exists() tells if the file/directory exists
    * canRead and canWrite Tells if you can read or write to the file
    * createNewFile() returns true if a new file is created, false if file already exists
    * delete() removes the file or directory
    *length()
    * mkdir() and mkdirs() to create a new directory
    * getFreeSpace()
    * createTempFile()
    * isHidden()
    * long lastModified()
    * String[] list() returns contents of directory in a String-array
    * File[] listFiles() returns contents of directory in a File-array
    *
    * */
}
